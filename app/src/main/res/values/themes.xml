<resources>
    <!-- Compose draws the whole UI, so a plain no-action-bar framework theme is enough. -->
    <style name="Theme.YouTubeBlocker" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
